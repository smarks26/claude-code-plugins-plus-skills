# Archived — MS-OLDV (Old-Version) Documents

These documents are tagged `MS-OLDV` per the filing standard
(`000-DR-STND-document-filing-system.md`) — they describe stale state,
typically from v1.0.0–v1.0.42 era. Repo is currently at v4.30.0+.

They were moved here on 2026-05-17 as part of the repo quality audit
(`000-docs/266-RA-AUDT-repo-quality-audit-2026-05-17.md`). Moved
instead of deleted so:

- The historical reasoning behind early decisions stays readable
- Outside auditors can still find them if they're doing repo
  archaeology
- The active `000-docs/` directory only surfaces currently-relevant
  documents to anyone browsing it

If you find yourself reading one of these for current guidance,
**stop** — find the current equivalent in the active docs first:

- For the v3.x → v4.x schema migration story →
  `SCHEMA_CHANGELOG.md`
- For the current spec → `6767-b-SPEC-DR-STND-claude-skills-standard.md`
- For the current contributor flow → `CONTRIBUTING.md` (repo root)
- For document filing → `000-DR-STND-document-filing-system.md`

To re-archive a stale doc in future, file it here with the same
`NNN-MS-OLDV-...` naming so the audit trail stays predictable.
